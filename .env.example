DATABASE_URL=
ADMIN_DATABASE_URL=
AUTH_SECRET=

# One-time first Firm Admin bootstrap
ALLOW_INITIAL_SETUP=false

# Encryption / KMS
# Use local only for development or controlled bootstrap environments.
KMS_PROVIDER=local
LOCAL_KMS_MASTER_KEY=
# Production Vault Transit alternative:
VAULT_ADDR=
VAULT_TOKEN=
VAULT_TRANSIT_KEY=taxbox
VAULT_TRANSIT_MOUNT=transit
VAULT_NAMESPACE=

# Railway Bucket (S3-compatible API)
AWS_S3_BUCKET_NAME=
AWS_ACCESS_KEY_ID=
AWS_SECRET_ACCESS_KEY=
AWS_ENDPOINT_URL=
AWS_DEFAULT_REGION=

# Email
EMAIL_PROVIDER=console
RESEND_API_KEY=
EMAIL_FROM=

# AI document classification
# Use openai in production; mock is useful for local UI testing.
DOCUMENT_AI_PROVIDER=openai
OPENAI_DOCUMENT_MODEL=gpt-5.6-luna
OPENAI_API_KEY=
